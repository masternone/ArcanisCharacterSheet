<table cellpadding="0" cellspacing="0" border="1" frame="void" rules="all" >
	<thead>
		<tr>
			<th>Name</th>
		</tr>
	</thead>
	<tbody><%
		if( talentGroup.length > 0 ){
			talentGroup.forEach(function( value, key ){
				var stripe = key % 2 == 0 ? 'even' : 'odd'; %>
				<tr class="<%= stripe %>">
					<td><a href="/talentGroup/<%= key %>"><%= value.name %></a></td>
					<% if( edit ){ %>
					<td><a href="<%= linkTo_talentGroupEdit %>"><%= linkTo_talentGroupEditText %></a></td>
					<% } %>
				</tr><%
			});
		}
	%></tbody>
</table>
